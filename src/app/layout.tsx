import type { Metadata } from "next";
import { Inter, Space_Grotesk } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800", "900"],
});

const spaceGrotesk = Space_Grotesk({
  variable: "--font-space-grotesk",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700"],
});

export const metadata: Metadata = {
  title: "SparkTech — Next-Gen EV Charging Solutions",
  description: "SparkTech pioneers smart EV charging infrastructure with AI-powered technology for a sustainable, electrified future.",
  keywords: "EV charging, electric vehicle, smart charging, SparkTech, sustainable energy",
  openGraph: {
    title: "SparkTech — Next-Gen EV Charging Solutions",
    description: "Pioneering smart charging solutions for a sustainable future.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} ${spaceGrotesk.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
